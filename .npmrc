# Enforce exact versions — no ^ or ~ ranges
save-exact=true

# Hoist dependencies for NestJS compatibility
shamefully-hoist=false

# Strict peer dependencies
strict-peer-dependencies=false

# Lock file
frozen-lockfile=false
